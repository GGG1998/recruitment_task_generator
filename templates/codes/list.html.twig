{% extends 'base.html.twig' %}
{% block body %}
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Lista kodów</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
            <div class="btn-group mr-2">
                <a href="{{ path('codes_create') }}" class="btn btn-sm btn-outline-secondary">Generuj</a>
                <a href="{{ path('codes_remove') }}" class="btn btn-sm btn-outline-secondary">Usuń</a>
            </div>

        </div>
    </div>
{% include 'codes/template_code.html.twig' %}
{% endblock %}